# Copy to .env.local for local development.

# Absolute origin the site is served from. Used for canonical URLs, sitemap,
# Open Graph images and Stripe redirect targets.
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
